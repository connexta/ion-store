callback-accept-version: 0.1.0-SNAPSHOT
logging:
  level:
    org.springframework.web.filter.CommonsRequestLoggingFilter: DEBUG
management:
  endpoints:
    web:
      exposure:
        include: "*"
        exclude: "/actuator"
