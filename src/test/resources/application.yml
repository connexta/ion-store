server:
  port: 8080
endpointUrl:
  retrieve: http://localhost:${server.port}/dataset/
  index: http://search:8080/index/
endpoints:
  index:
    version: testIndexApiVersion
  store:
    version: testStoreApiVersion
  transform:
    version: transformApiVersion
s3:
  bucket: ingest-quarantine